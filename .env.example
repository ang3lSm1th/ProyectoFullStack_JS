# Web (Next.js)
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# API (NestJS)
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

# PostgreSQL (local)
DATABASE_URL=postgresql://portfolio:portfolio@localhost:5432/portfolio?schema=public
DIRECT_URL=postgresql://portfolio:portfolio@localhost:5432/portfolio?schema=public
POSTGRES_USER=portfolio
POSTGRES_PASSWORD=portfolio
POSTGRES_DB=portfolio
POSTGRES_PORT=5432

# GitHub (puente de proyectos públicos)
GITHUB_USERNAME=ang3lSm1th
# Opcional: PAT con scope repo para incluir privados. No commitear.
# GITHUB_TOKEN=

# Supabase (cuando la API apunte a la nube)
# En Dashboard → Project Settings → Database → Connect:
# - DIRECT_URL = Direct connection (puerto 5432, host db.<ref>.supabase.co)
# - DATABASE_URL = Session pooler (puerto 5432, host *.pooler.supabase.com)
# Añade ?schema=public&sslmode=require al final de ambas.
